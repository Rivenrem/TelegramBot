const { BOT_TOKEN } = process.env;
const { DB_CONN_STRING } = process.env;
const { OPENTRIP_API_KEY } = process.env;
const { OPENTRIP_STATIC_URL } = process.env;
const { PHOTOS_API_KEY } = process.env;
const { PHOTO_STATIC_URL } = process.env;
const { PORT } = process.env;
const { WEATHER_API_KEY } = process.env;
const { WEATHER_STATIC_URL } = process.env;
const { GOOGLE_MAPS_SEARCH_STATIC_URL } = process.env;

export const envVariables = {
    BOT_TOKEN,
    DB_CONN_STRING,
    OPENTRIP_API_KEY,
    OPENTRIP_STATIC_URL,
    PHOTOS_API_KEY,
    PHOTO_STATIC_URL,
    PORT,
    WEATHER_API_KEY,
    WEATHER_STATIC_URL,
    GOOGLE_MAPS_SEARCH_STATIC_URL,
};
